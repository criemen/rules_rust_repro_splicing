# load("@crate_index//:defs.bzl", "aliases", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "main",
    srcs = ["src/main.rs"],
    # aliases = aliases(),
    # deps = all_crate_deps(
    #     normal = True,
    # ),
)
